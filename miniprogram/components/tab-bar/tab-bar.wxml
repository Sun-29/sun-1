<view class="tab-bar">
  <view wx:for="{{tabs}}" wx:key="key" class="tab-item {{active === item.key ? 'active' : ''}}" data-page="{{item.page}}" bindtap="switchTab">
    <view class="tab-icon">{{item.icon}}</view>
    <view class="tab-label">{{item.label}}</view>
  </view>
</view>
